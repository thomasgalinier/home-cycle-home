# Frontend runtime config (used by docker-compose)
# The PORT maps host -> container:  PORT:80
PORT=8080

# Build-time Vite variables (available via import.meta.env.VITE_*)
# Set to your backend API URL (e.g., http://localhost:3000 or https://api.example.com)
VITE_API_URL=http://localhost:3000

# Add any other sensitive values needed at build time
# VITE_SOME_KEY=change-me
